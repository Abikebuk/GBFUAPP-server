/**
 * configLoader.ts
 * charges variables into the api
 * TODO: clean this part
 */
import TWITTER from '../config/twitter';
import RAIDS from '../config/raids.json';
import SERVER from '../config/server';
const CONFIG = {
    TWITTER,
    SERVER,
    RAIDS,
};

export default CONFIG;
